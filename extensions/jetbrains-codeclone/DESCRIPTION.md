Native JetBrains IDE surface for codeclone-mcp — enterprise tool window UI with tables, dashboards, and HTML session insights.
Triage-first, repository read-only, driven by the same canonical report as the CLI and VS Code extension.
