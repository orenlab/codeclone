{
  "name": "@orenlab/codeclone-claude-desktop",
  "version": "2.0.0-b5.1",
  "private": true,
  "description": "Claude Desktop MCP bundle wrapper for the local CodeClone MCP launcher.",
  "license": "MPL-2.0",
  "type": "commonjs",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "check": "node --check server/index.js && node --check src/launcher.js && node --check scripts/build-mcpb.mjs",
    "test": "node --test test/*.test.js",
    "pack": "node scripts/build-mcpb.mjs"
  }
}
